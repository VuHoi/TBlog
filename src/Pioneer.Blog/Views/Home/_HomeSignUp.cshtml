@model Pioneer.Blog.Model.Views.SignUpViewModel
<section class="home-sign-up" id="home-sign-up">
  <ul style="display: none"><li></li></ul>
  <div class="row">
    <div class="large-10 large-push-1 columns">
      <h4><span>Tin tức  công nghệ mới nhất sẽ được gửi thẳng vào hộp thư của  bạn</span></h4>
      <p>Luôn cập nhật về tất cả các sự kiện xảy ra tại KHOA HỌC CÔNG NGHỆ, nhận các đề nghị độc quyền và là người đầu tiên biết về nội dung mới nhất.</p>
      <form asp-controller="Home" asp-action="SignUp" method="post" data-abide novalidate>
        <div data-abide-error class="alert callout" style="display: none;">
          <p>
            <i class="fa fa-alert"></i>Bạn cần cung cấp một email còn sử dụng được
          </p>
        </div>
        @if (ViewBag.IsValid != null && !ViewBag.IsValid)
        {
          if (ViewBag.IsValidMessage != null)
          {
                <div class="callout alert">
                  <p>
                    @ViewBag.IsValidMessage
                  </p>
                </div>
          }
          else
          {
                <div class="callout alert">
                  <div asp-validation-summary="All"></div>
                </div>
          }
        }
        @if (ViewBag.IsValid != null && ViewBag.IsValid)
        {
            <div class="callout success">
              <p>Cảm ơn đã đăng kí!</p>
            </div>
        }
        <div>
          <div class="input-group">
            <input asp-for="Email" placeholder="Địa chỉ email của bạn..." pattern="email" required class="input-group-field" />
            <div class="input-group-button">
              <button type="submit" class="button">
                <i class="fa fa-envelope-o"></i>&nbsp;SEND
              </button>
            </div>
          </div>
        </div>
      </form>
    </div>
  </div>
</section>